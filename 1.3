#include <stdio.h>

int main(void) {
    int a1, a2, a3, b1, b2, c1, c2, c3, c4;
    scanf("%d, %d, %d, %d, %d", &a1, &a2, &a3, &b1, &b2);
    
    c1 = a1+b1;
    c2 = a2+b2+(c1/10);
    c1 %= 10;
    c3 = a3 + (c2/10);
    c2 %= 10;
    c4 = (c3/10);
    c3 %= 10;
    printf("%d, %d, %d, %d", c4, c3, c2, c1);
    
    return 0;
}
