#include <stdio.h>

int main(void) {
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);

    if(((a / c) * (b / d)) > ((a / d) * (b / c)))
        printf("vdol dlinnoi\n");
    else
        printf("vdol korotkoi\n");
    printf("%d %d", (a / c) * (b / d), (a / d) * (b / c));

    return 0;
}
