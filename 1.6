#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int h, m;
    scanf("%d %d", &h, &m);

    printf("%d", ((h % 12 * 5 + 15 * (h % 12 * 5 + 15 - m) / abs((h % 12 * 5 + 15 - m))) + 60 - m) % 60);
    return 0;
}
