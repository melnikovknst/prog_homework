#include <stdio.h>
#include <math.h>


int main(void){
    float y, hoursAngle;
    scanf("%f", &y);
    hours_angle = (y * 180 / M_PI);

    printf("%f ", (hoursAngle - 30 * (floor)(hoursAngle / 30)) * 12);  //Minutes angle
    printf("%d ", (int)(hoursAngle / 30));  //Hours
    printf("%d\n", (int)((hoursAngle - 30 * (floor)(hoursAngle / 30)) * 12)/6); //Minutes

    return 0;
}
