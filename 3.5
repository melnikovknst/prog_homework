#include <stdio.h>
#include <math.h>


int main(void) {
    float s = 0.0;
    
    for (int i = 10; i > 0; i--)
        s += M_PI / 10 * sin(M_PI / 10 * i - M_PI / 20);
    printf("%f\n", s);
    
    return 0;
}
