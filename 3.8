#include <stdio.h>


int main(void) {
    for (int a = 0; a < 4; a++) {
        for (int b = 0; b < 4; b++) {
            for (int c = 0; c < 4; c++) {
                for (int d = 0; d < 4; d++) {
                    float sum = 1.0;
                    if (a == 0) 
                        sum += 2;
                    else if (a == 1)
                        sum -= 2;
                    else if (a == 2)
                        sum *= 2;
                    else
                        sum /= 2;
                    if (b == 0) 
                        sum += 3;
                    else if (b == 1)
                        sum -= 3;
                    else if (b == 2)
                        sum *= 3;
                    else
                        sum /= 3;
                    if (c == 0)
                        sum += 4;
                    else if (c == 1)
                        sum -= 4;
                    else if (c == 2)
                        sum *= 4;
                    else
                        sum /= 4;
                    if (d == 0)
                        sum += 5;
                    else if (d == 1)
                        sum -= 5;
                    else if (d == 2)
                        sum *= 5;
                    else
                        sum /= 5;
                    if (sum == 35)
                        printf("%d %d %d %d %f\n", a, b, c, d, sum);
                }
            }
        }
    }
    return 0;
}

