#include <stdio.h>


int main(void) {
    int digit;
    scanf("%d", &digit);
    
    for(int cucle = 5; cucle > 0; cucle--){
        printf("%d\n", digit % 10);
        digit /= 10;
    }

    return 0;
}
