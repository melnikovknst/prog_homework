#include <stdio.h>
#include <math.h>
#define e 2.7182818284590451


int main(void) {
    float p0 = 1.29, z = 1.25 * pow(10,-4), p = 1.1, h;
    
    for (h = 0; p >= 1.0; h += 0.001) p = p0 * pow(e, -h * z);
    printf("%f\n", h);
    
    return 0;
}
