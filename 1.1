#include <stdio.h>


int main(void) {
    float a, c, b;
    scanf("%f", &a);
    
    a *= a; //2
    a *= a; //4
    b = a;
    a *= a; // 8
    c = a;
    a *= a;   // 16
    a = a * b * c;   // 16 + 4 + 8
    printf("%f\n",a);
    
    return 0;
}
