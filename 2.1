#include <stdio.h>


int main(void) {
    int k;
    scanf("%d", &k);

    if ((k%10 == 2) || (k%10 == 3) || (k%10 == 4))
        printf("My nashly %d griba\n", k);
    else if (k%10 == 1)
        printf("My nashly %d grib\n", k);
    else
        printf("My nashly %d gribov\n", k);

    return 0;
}
